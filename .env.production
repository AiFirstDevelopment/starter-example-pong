# Where the built site looks for its table server.
#
# The site is on Cloudflare Pages and the tables are Durable Objects, which
# cannot live there — so the two are deployed separately and the bundle has to
# be told the second address at build time. `npm run deploy` builds with this.
VITE_TABLE_URL=wss://pong-table.joelstevick.workers.dev
